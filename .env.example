# ============================================================================
# 环境变量配置示例文件
# ============================================================================
# 
# 使用方法：
# 1. 复制此文件为 .env
# 2. 替换下面的值为你的实际配置
# 3. 或者在运行前直接在终端设置环境变量
# 
# Linux/macOS:
#   export WECHAT_APP_ID=your_app_id
#   ...
# 
# Windows (PowerShell):
#   $env:WECHAT_APP_ID='your_app_id'
#   ...
# ============================================================================

# ----------------------------------------------------------------------------
# 微信公众号配置
# ----------------------------------------------------------------------------
# 微信公众号的AppID
WECHAT_APP_ID=your_app_id_here

# 微信公众号的AppSecret
WECHAT_APP_SECRET=your_app_secret_here

# 是否通过 ECS 中转上传素材 (true/false)
WECHAT_UPLOAD_VIA_ECS=true

# ECS 网关地址 (如果在内网或特定服务器)
WECHAT_ECS_GATEWAY_URL=http://your_ecs_ip:8000

# ----------------------------------------------------------------------------
# 钉钉配置
# ----------------------------------------------------------------------------
# 钉钉应用的AppKey
DINGTALK_APP_KEY=your_app_key_here

# 钉钉应用的AppSecret
DINGTALK_APP_SECRET=your_app_secret_here

# 钉钉用户ID（用于API调用）
DINGTALK_USER_ID=your_user_id_here

# ----------------------------------------------------------------------------
# AI 风格化提示词配置
# ----------------------------------------------------------------------------
# 个人风格提示词
PERSONAL_STYLE_PROMPT=尽量不要修改原文的内容。

# 各平台特定提示词
PLATFORM_STYLE_PROMPT_WECHAT=尽量不要修改原文的内容，内容不要改写，可是需要修改原文的排版格式，使其适配微信公众号的文章格式，必须为markdown的格式输出。
PLATFORM_STYLE_PROMPT_XIAOHONGSHU=尽量不要修改原文的内容，使其适配小红书长文章的格式，必须为markdown的格式输出。并且假如文章中出现链接，提取出可以在浏览器搜索到这个链接内容的关键词的搜寻方法代替链接
PLATFORM_STYLE_PROMPT_ZHIHU=
PLATFORM_STYLE_PROMPT_CSDN=
PLATFORM_STYLE_PROMPT_51CTO=
PLATFORM_STYLE_PROMPT_CNBLOGS=
PLATFORM_STYLE_PROMPT_DOUYIN=
PLATFORM_STYLE_PROMPT_KUAISHOU=
PLATFORM_STYLE_PROMPT_BILIBILI=

# ----------------------------------------------------------------------------
# 小红书自动化选择器配置 (Playwright Selectors)
# ----------------------------------------------------------------------------
# 创作中心入口
XHS_LONGFORM_MODE_SELECTOR=span.center:has-text("新的创作")
# 标题输入框
XHS_LONGFORM_TITLE_SELECTOR=textarea.d-text[placeholder="输入标题"]
# 正文输入框
XHS_LONGFORM_BODY_SELECTOR=p[data-placeholder="粘贴到这里或输入文字"]
# 一键排版按钮
XHS_LONGFORM_FORMAT_SELECTOR=span.next-btn-text:has-text("一键排版")
# 下一步按钮
XHS_LONGFORM_NEXT_SELECTOR=button.d-button.custom-button.submit:has-text("下一步")
# 标签输入框
XHS_LONGFORM_TAG_INPUT_SELECTOR=p[data-placeholder="输入正文描述，真诚有价值的分享予人温暖"]
# 发布按钮
XHS_LONGFORM_PUBLISH_SELECTOR=button.custom-button.bg-red:has-text("发布")
